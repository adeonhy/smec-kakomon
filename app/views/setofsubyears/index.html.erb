<ul class="breadcrumb">
    <li class="active">Home</li>
</ul>

<h2>年度から選ぶ</h2>
<table class="table table-striped">
    <thead>
        <tr><th>科目</th><th>2012</th><th>2011</th><th>2010</th><th>2009</th><th>2008</th></tr>
    </thead>
    <tbody>
        <% @theme_by_subject.each do |st| %>
            <tr>
                <td><%= st[:kamoku] %></td>
                <% %w(2012 2011 2010 2009 2008).each do |y| %>
                    <td>
                        <% @setofsubyears.select {|i|(i.subject == st[:kamoku]) && (i.year == y)}.each do |set| %>
                            <% if set.available %>
                                <%= link_to content_tag('i','',class: "icon-book"), "/q/#{st[:code]}/#{set.year}" %>
                            <% else %>
                                -
                            <% end %>
                        <% end %>
                    </td>
                <% end %>
            </tr>
        <% end %>
    </tbody>
</table>
        <tr><td>経済学・経済政策</td><td>
                <% @setofsubyears.select {|i| i.subject == "経済学・経済政策"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/q/a/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>財務・会計</td><td>
                <% @setofsubyears.select {|i| i.subject == "財務・会計"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/q/b/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>企業経営理論</td><td>
                <% @setofsubyears.select {|i| i.subject == "企業経営理論"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>運営管理</td><td>
                <% @setofsubyears.select {|i| i.subject == "運営管理"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>経営法務</td><td>
                <% @setofsubyears.select {|i| i.subject == "経営法務"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>経営情報システム</td><td>
                <% @setofsubyears.select {|i| i.subject == "経営情報システム"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
        <tr><td>中小企業経営・政策</td><td>
                <% @setofsubyears.select {|i| i.subject == "中小企業経営・政策"}.each do |set| %>
                    <% if set.available %>
                        <%= link_to set.year, "/#{set.year}" %>
                    <% else %>
                        <%= set.year %> 
                    <% end %>
                <% end %>
        </td></tr>
    </tbody>
</table>

<h2>テーマから選ぶ</h2>

<table class="table table-striped">
    <thead>
    </thead>
    <tbody>
        <% @theme_by_subject.each do |st| %>
            <tr>
                <td><h4><%=st[:kamoku] %></h4>
                    <% st[:themes].each do |mt| %>
                        <span class="tags"
                        <i class="icon-tag"></i><%= link_to mt.maintheme, "/theme/#{mt.themecode[0..2]}" %>
                        <!--
                        <span class="badge badge-success" style="margin-right:10px">
                            <%= @mainthemescount[mt.themecode[0..2]] %>
                        </span>
                        -->
                        (<%= @mainthemescount[mt.themecode[0..2]] %>)
                        </span>
                    <% end %>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>

